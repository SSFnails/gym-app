/**
 * Одно движение под двумя идентификаторами. Такое бывает: в пуле сборки
 * программы движение называется одним ключом, в списке замен — другим,
 * а картинка у них одна и та же.
 *
 * Псевдоним вместо копии файла: две одинаковые картинки в сборке — это
 * лишние килобайты в офлайн-кеше и второй файл, который забудут обновить.
 * Лежит отдельным модулем, потому что его читает и приложение,
 * и scripts/audit-images.mjs — знание должно быть в одном месте.
 */
export const IMAGE_ALIAS: Record<string, string> = {
  // «Тяга в тренажёре с упором в грудь» — она же chest-supported-row в заменах.
  'chest-row': 'chest-supported-row',
};
