# Задачи

- [ ] 1. Таблица `profiles` и поле `profileId` в личных таблицах
  - добавить в `src/db/db.ts` версию 5 схемы
  - индексы по `profileId` там, где по нему идёт выборка
  - _Требования: 1, 2_

- [ ] 2. Миграция существующих данных
  - создать профиль владельца, проставить `profileId` всем строкам
  - записать активный профиль в настройки
  - **тест**: база с данными до миграции сохраняет всё после
  - _Требования: 2_

- [ ] 3. Выборки с учётом активного профиля
  - `session.ts`, `Home`, `Progress`, `Settings` читают только свой профиль
  - _Требования: 2_

- [ ] 4. Экран создания профиля
  - восемь шагов, по вопросу на экран, кнопка «назад» на каждом
  - валидация: рост 120–230, вес 30–200, стаж 0–40
  - _Требования: 1, 3, 6_

- [ ] 5. Сборка программы для нового профиля
  - `generateProgram` → запись `days`, `exercises`, `exerciseState`
  - показать `gaps`, если слоты закрыть нечем
  - _Требования: 3_

- [ ] 6. Блок «Профиль» в настройках
  - переключение, создание, удаление с подтверждением внутри приложения
  - _Требования: 5_

- [ ] 7. Пересчёт весов под свои данные
  - предпросмотр «было → станет», применение только после подтверждения
  - _Требования: 4_

- [ ] 8. Проверка
  - `npm run build`, оба набора тестов
  - `node scripts/walkthrough.mjs --sessions=2` проходит
  - снимок с `--offline` без ошибок в консоли
